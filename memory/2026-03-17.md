# 2026-03-17 — Session Notes

## What happened
- Added V3DN reminder to action queue ($400 lent from 7089 to Dad wallet)
- Discovered Blob store suspended (billing: Inactive)
- Brain directive: drop Blob entirely, switch to deploy-time snapshots
- Built Ricky (Brain) tab — full operations view with 7 sections:
  - Vercel Projects (19, with duplicate flagging)
  - Active Crons (11, with status/schedule/model)
  - Agent Fleet (model, sessions, context %, staleness)
  - Services & Costs (~$28.80/mo + $144/yr domains)
  - Infrastructure (Mac Mini, ports, gateway)
  - Claude Cost via CodexBar ($5.44 session / $8.53 30d)
  - Bitwarden Vault (folder names only)
- Stripped @vercel/blob, rewrote push-snapshot.js to bundle + deploy
- Rewrote API route to read bundled snapshot only
- Updated cron to generate + deploy (timeout 180s)
- Brain reviewed, applied 2 minor fixes, approved deploy
- Deployed live

## CodexBar
- Installed by Brain, `codexbar cost --provider claude --json` works
- Gives real cost data: daily breakdown by model, 30-day totals, session totals
- Wired into push-snapshot.js → claudeCost in snapshot → Ricky tab

## Architecture change
- OLD: Mac Mini → push-snapshot.js → Vercel Blob → API reads Blob
- NEW: Mac Mini → push-snapshot.js → bundle public/snapshot.json → vercel --prod → API reads bundled file
- Simpler, no external dependency, no billing issues

## Git
- Final: 5fbd21e1 on agent/command-centre, pushed

## Compactions
- Zero compactions this session

## Context at session end
- 74% (148k/200k) before this final write
